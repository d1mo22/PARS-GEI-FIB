#include <iostream>
#include <vector>
using namespace std;

bool parell(const vector<int>& v) {

}

int main() {
    int n;
    while (cin >> n) {
        vector <int> v(n);
        for (int i = 0; i < v.size(); ++i) cin >> v[i];
        
    }
    
}